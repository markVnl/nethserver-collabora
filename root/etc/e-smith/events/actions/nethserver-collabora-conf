#!/bin/bash

# Get domain from prop
if [[ -n `config getprop loolwsd Domain` ]]; then loolconfig set storage.wopi.host `config getprop loolwsd Domain`;fi

# Disable ssl
loolconfig set ssl.enable  false
loolconfig set ssl.termination  true
loolconfig set net.listen loopback
